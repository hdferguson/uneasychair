<%= form_for(committee) do |f| %>
  <% if committee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(committee.errors.count, "error") %> prohibited this committee from being saved:</h2>

      <ul>
      <% committee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  
  
   <div class="field">
    <%= f.label :user_id %>
       <%= f.collection_select :user_id, User.order(:name),:id,:name, include_blank: false %>
  </div>
  

  <div class="field">
    <%= f.label :conference_id %>
       <%= f.collection_select :conference_id, Conference.order(:name),:id,:name, include_blank: false %>

  </div>
  
 


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
